# Copy to .env and adjust. The .env file is gitignored — never commit secrets.

# Your assigned, pre-configured workshop org alias (set with: sf org login web --alias <alias>)
ORG_ALIAS=ncino-workshop

# The shared hosted mock MCP server. Do NOT change unless your facilitator says so.
MCP_SERVER_URL=https://ncino-banking-advisor-mock.bstauber.workers.dev

# Client id/secret for the MCP External Credential principal are provided by your
# facilitator and entered in the Salesforce UI — they are NOT stored here.
